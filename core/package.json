{
  "name": "@echo/core",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Echo core — one codebase, two processes (api, worker). M1.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "api": "node --experimental-strip-types src/api/main.ts",
    "worker": "node --experimental-strip-types src/worker/main.ts"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.84.1",
    "@earendil-works/pi-ai": "^0.84.1",
    "fastify": "^5.1.0",
    "pino": "^9.5.0",
    "postgres": "^3.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
